name: Build

# Builds the image and publishes it, so the thing the README describes can
# actually be obtained. Until 2026-09-13 it could not: the image existed only
# as a locally built layer on one server, which makes every install
# instruction in this repo a description of something nobody else has.

on:
  push:
    branches: [main]
    tags: ["v*"]
    paths-ignore:
      - "**/*.md"
      - ".github/assets/**"
  pull_request:
    paths-ignore:
      - "**/*.md"
      - ".github/assets/**"
  workflow_dispatch:

permissions:
  contents: write   # release asset upload
  packages: write

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE: ghcr.io/${{ github.repository }}

jobs:
  build:
    name: Build and publish (amd64)
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      # The finished image is around 5 GB: two browsers plus a Go, Node and
      # Python toolchain on top of the Selkies base. A stock runner is tight
      # once the build cache is added, and the failure arrives late and reads
      # as a broken download rather than a full disk.
      - name: Make room on the runner
        run: |
          df -h / | tail -1
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
                      /usr/local/share/boost "$AGENT_TOOLSDIRECTORY" || true
          sudo apt-get clean || true
          df -h / | tail -1

      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4

      # Push only on main, tags and manual runs, never from a pull request: a
      # fork PR gets no secrets, and a push from unreviewed code is not wanted
      # even when it would work.
      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Docker Hub mirror, active only once DOCKERHUB_USERNAME (variable) and
      # DOCKERHUB_TOKEN (secret) are set together. Without them the run stays
      # green and publishes to GHCR alone, so the mirror can be switched on
      # later without touching this file.
      - name: Log in to Docker Hub
        if: ${{ github.event_name != 'pull_request' && vars.DOCKERHUB_USERNAME != '' }}
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Is the tag being built the newest release in this repo?
      #
      # `latest` moves only for a tag build, and only for the highest version.
      # Both conditions are needed and they guard different accidents. Gating on
      # the tag stops an unreleased merge to main from reaching everyone who
      # pulls `latest` before a release exists. Gating on "newest" stops a re-cut
      # of an older version tag from dragging `latest` backwards onto it.
      #
      # checkout fetches no tags, so they are fetched here rather than assumed,
      # and `sort -V` orders versions instead of strings, so v1.10.0 beats
      # v1.9.0.
      - name: Is this the newest release tag?
        id: newest
        if: github.ref_type == 'tag'
        run: |
          git fetch --tags --force --quiet
          newest=$(git tag -l 'v[0-9]*.[0-9]*.[0-9]*' | sort -V | tail -1)
          echo "this tag: ${GITHUB_REF_NAME}"
          echo "newest:   ${newest}"
          if [ "$newest" = "${GITHUB_REF_NAME}" ]; then
            echo "is_newest=true" >> "$GITHUB_OUTPUT"
            echo "-> this build moves :latest"
          else
            echo "is_newest=false" >> "$GITHUB_OUTPUT"
            echo "-> this build leaves :latest where it is"
          fi

      - name: Docker metadata (tags + labels)
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          images: |
            ${{ env.IMAGE }}
            name=docker.io/${{ vars.DOCKERHUB_USERNAME }}/${{ github.event.repository.name }},enable=${{ vars.DOCKERHUB_USERNAME != '' }}
          # `latest` means "the newest released version", and nothing else.
          #
          # It used to mean "the tip of main", on the reasoning that main is at
          # or ahead of the newest tag whenever a release is cut. That is true
          # and it is the problem: on 2026-09-14 a 23-commit merge to main put
          # an unreleased build in front of everyone pulling `latest` before any
          # release existed, and the first bug report against it arrived before
          # the release notes did.
          #
          # So `latest` is set by a tag build, and only when that tag is the
          # newest release (see the step above). Gating on the tag alone would
          # bring back the opposite accident: on 2026-09-02 a re-cut of an old
          # version tag pulled `latest` backwards onto it. That is why there are
          # two conditions rather than one.
          #
          # The tip of main gets `edge` instead. Still built, still pushed,
          # still pullable, just nobody lands on it without asking for it.
          tags: |
            type=sha,format=short
            type=raw,value=edge,enable=${{ github.ref_type == 'branch' && github.ref_name == github.event.repository.default_branch }}
            type=raw,value=latest,enable=${{ github.ref_type == 'tag' && steps.newest.outputs.is_newest == 'true' }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}

      # amd64 only, and that is not a shortcut. Google Chrome ships no arm64
      # Linux build, and the box this is for is an x86_64 machine with an
      # NVIDIA card. An arm64 image would be missing half of what the README
      # promises and would quietly suggest otherwise by existing.
      - name: Build and push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false

      # The download button in the README points at
      # releases/latest/download/docker-compose.yml, and that URL only resolves
      # if the file is a release ASSET. A raw file in the repo would open as
      # text in a tab instead of downloading, because raw.githubusercontent.com
      # serves it as text/plain; a release asset carries
      # Content-Disposition: attachment and therefore actually downloads.
      - name: Attach the compose file to the release
        if: github.ref_type == 'tag'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${{ github.ref_name }}" docker-compose.yml --clobber

      - name: Say where it went
        if: github.event_name != 'pull_request'
        run: |
          echo "### Published" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          echo "${{ steps.meta.outputs.tags }}" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
