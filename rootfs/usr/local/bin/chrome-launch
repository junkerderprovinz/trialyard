#!/bin/sh
# Chrome's SUID sandbox helper needs privileges this unprivileged container does
# not have, and the container is already the isolation boundary, as in the
# Selenium and Playwright images. --disable-dev-shm-usage is not needed since
# the container runs with --shm-size=1gb.
exec google-chrome-stable --no-sandbox "$@"
